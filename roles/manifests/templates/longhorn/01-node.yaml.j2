{% for node, disks in manifests_longhorn.disks.items() %}
---
apiVersion: v1
kind: Node
metadata:
  name: {{ node }}
  labels:
    node.longhorn.io/create-default-disk: config
  annotations:
    node.longhorn.io/default-disks-config: '{{ disks | to_json }}'
{% endfor %}